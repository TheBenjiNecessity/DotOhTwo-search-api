services:
  api:
    build: .
    ports:
      - "8080:8080"
    networks:
      - dotohtwolocalinfra
    environment:
      ELASTICSEARCH_URIS: http://elasticsearch:9200
      TMDB_API_KEY: ${TMDB_API_KEY:-}

networks:
  dotohtwolocalinfra:
    external: true
